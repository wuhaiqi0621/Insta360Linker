# FFmpeg Runtime

Insta360Linker expects the Windows FFmpeg executable at:

```text
assets/ffmpeg/ffmpeg.exe
```

The executable is intentionally excluded from Git because it is a large third-party runtime binary. Copy a compatible FFmpeg build to this path before using live preview or video thumbnail generation.
